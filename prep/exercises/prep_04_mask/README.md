# P04 · 做一条软边

`smoothstep` 会把一个范围内的数平滑地压到 `0` 到 `1`。把它当成遮罩，就能做出没有硬边的过渡。

## 任务

让画面左侧为黑色，右侧为白色，中间用 `smoothstep` 平滑过渡。

## 验收

- 左侧接近黑色，右侧接近白色。
- 过渡连续，没有突然跳变。

## 提示 1

使用 `UV.x` 作为输入。

## 提示 2

`smoothstep(edge0, edge1, value)` 会返回 `0` 到 `1`。

## 提示 3

把遮罩复制到 RGB 三个通道：`vec3(mask)`。
